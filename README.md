# GrindTrack — Study Tracker MVP

Minimal Next.js + Supabase MVP to track focused study sessions.

Quick start

1. (Local mode) npm install
2. npm run dev

This repo is now configured to run locally without Supabase. Sessions and auth are stored in `localStorage` for rapid local development. For production or multi-user use, integrate Supabase or another backend.
